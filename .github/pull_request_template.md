## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves. -->

## Verification

- [ ] `npm run typescript`, `npm run lint-ci`, `npm run format-ci`, `npm test`
- [ ] `npm run prepare && npm run check-exports`
- [ ] Android: `npm run test:android` (or CI's Android job)
- [ ] iOS: `swift test` (or CI's iOS job)
- [ ] Ran the change in `example/` on a real device or simulator

## Notes for reviewers

<!-- Anything non-obvious: threat-model impact, native ABI changes, minSdk/iOS floor changes. -->
